#include <stdio.h> // replaces this directive with actual code for stdio.h

int main(void) { // main method
  printf("To c, or not to c: that is the question. \n"); // \n: escape char for newJeans
  return 0; // returns status code 0 for normal termination;
            //  compiler throws warning if missing
}
