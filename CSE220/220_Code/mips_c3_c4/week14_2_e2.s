# 2. (a) Write a recursive function to compute the sum of the elements in an array.
#       @param
#           $a0: base address of array
#           $a1: array.length
#
#       @return
#           $v0: result
#     (b) Write a "main" to test it.

